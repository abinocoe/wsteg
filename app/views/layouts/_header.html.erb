<% @links.each do |link| %>
  <%= link_to link.title, page_path(link) %>
<% end %>
<%= link_to 'Indexamples', root_path %>
<%= link_to 'Indexpertise', page_path(@indexpertise) %>
<%= link_to 'Log out', destroy_user_session_path, method: :delete if current_user %>

