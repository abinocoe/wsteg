<h1><%= @page.title  %></h1>
<div><%= @page.body.html_safe  %></div>

<%= link_to 'Edit', edit_page_path(@page) %>
<%= link_to 'Delete', page_path(@page), method: :destroy, data: { confirm: 'Are you sure you want to delete this page, Steve? This action cannot be undone.' } %>


